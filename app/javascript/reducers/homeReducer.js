// app/javascript/reducers/homeReducer.js
const initialState = {
  message: "New rails react app"
};

const homeReducer = (state = initialState, action) => {
  return state;
};

export default homeReducer;
